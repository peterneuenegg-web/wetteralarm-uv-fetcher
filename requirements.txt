# ICON-CH1 via offizielle MeteoSchweiz-Lib (bringt eccodes/earthkit/xarray/pyproj mit).
# Python 3.10–3.12 (NICHT 3.13) — siehe Workflow.
meteodata-lab==0.7.2

# Direkt importiert im Worker:
scipy==1.14.1          # cKDTree fuer Nearest-Cell
requests==2.32.3

# numpy/xarray kommen transitiv ueber meteodata-lab in kompatibler Version —
# bewusst NICHT gepinnt, um Resolver-Konflikte mit meteodata-lab 0.7.2 zu vermeiden.
